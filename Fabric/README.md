# DevOps-Deploy

Create conda env
```
conda create --yes --quiet --name fn_devops python=3.6
```

Go to conda env
```
conda activate fn_devops
```

Install Fabric
```
Fabric3==1.14.post1 or
pip install -r requirements.txt
```
